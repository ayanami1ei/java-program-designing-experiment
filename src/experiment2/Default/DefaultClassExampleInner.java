package experiment2.Default;

public class DefaultClassExampleInner {
    public static void main(String[] args) {
        DefaultClass d = new DefaultClass();
        //System.out.println(d.defaultData+" "+d.protectedData+" "+d.privateData+" "+d.publicData);
    }
}