package experiment2.Public;

public class PublicClassExampleInner {
    public static void main(String[] args) {
        PublicClass d = new PublicClass();
        //System.out.println(d.defaultData+" "+d.protectedData+" "+d.privateData+" "+d.publicData);
    }
}
